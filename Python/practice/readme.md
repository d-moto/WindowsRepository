## Python 練習問題

### 1. 変数宣言
1.  変数xを宣言し、値として10を代入してください。
2.  変数nameを宣言し、値として自分の名前を代入してください。
3.  変数is_trueを宣言し、値としてTrueを代入してください。
4.  変数piを宣言し、値として円周率π(3.14159)を代入してください。
5.  変数fruitsを宣言し、値としてリスト['apple', 'banana', 'orange']を代入してください。

### 2. printで出力
1.  変数xの値を表示してください。
2.  変数nameの値を表示してください。
3.  変数is_trueの値を表示してください。
4.  変数piの値を表示してください。
5.  変数fruitsの値を表示してください。

### 3. 数値
1.  変数aに数値10を代入し、変数bに数値5を代入してください。aとbの和を表示してください。
2.  変数aとbの差を表示してください。
3.  変数aとbの積を表示してください。
4.  変数aを変数bで割った商を表示してください。
5.  変数aを変数bで割った余りを表示してください。

### 4. 文字列
1.  変数messageに文字列"Hello, world!"を代入してください。変数messageの値を表示してください。
2.  変数nameに自分の名前を代入し、文字列"My name is "と変数nameの値を結合して表示してください。
3.  変数languageに文字列"Python"を代入し、文字列"I love "と変数languageの値を結合して表示してください。
4.  変数word1に文字列"Hello"を、変数word2に文字列"world"を代入し、word1とword2をスペースで区切って表示してください。
5.  変数quoteに文字列"To be or not to be"を代入し、文字列の長さを表示してください。

### 5. 文字列とインデックス
1.  変数sentenceに文字列"I love Python programming."を代入してください。変数sentenceの先頭の文字を表示してください。
2.  変数sentenceの末尾の文字を表示してください。
3.  変数sentenceのインデックス2の文字を表示してください。
4.  変数sentenceのインデックス6から12までの部分文字列を表示してください。
5.  変数sentenceのインデックスを逆順にした文字列を表示してください。

### 6. 文字のメソッド
1.  変数wordに文字列"Hello"を代入してください。変数wordをすべて小文字に変換して表示してください。
2.  変数wordをすべて大文字に変換して表示してください。
3.  変数wordの文字列の長さを表示してください。
4.  変数wordに文字列"Hello, world!"を代入してください。変数wordの先頭が大文字かどうかを判定して、結果を表示してください。
5.  変数wordに文字列" Spaces all around "を代入してください。変数wordの両端の空白を除去した文字列を表示してください。

### 7. 文字の代入
1.  変数nameに自分の名前を代入してください。変数messageに文字列"My name is {name}."を代入し、{name}の部分を変数nameの値に置き換えて表示してください。
2.  変数ageに自分の年齢を代入してください。変数messageに文字列"I am {age} years old."を代入し、{age}の部分を変数ageの値に置き換えて表示してください。
3.  変数fruitに好きな果物の名前を代入してください。変数messageに文字列"I love {fruit}."を代入し、{fruit}の部分を変数fruitの値に置き換えて表示してください。
4.  変数priceに商品の値段を代入してください。変数messageに文字列"The price is {price} dollars."を代入し、{price}の部分を変数priceの値に置き換えて表示してください。
5.  変数quantityに商品の個数を代入してください。変数messageに文字列"You have {quantity} items."を代入し、{quantity}の部分を変数quantityの値に置き換えて表示してください。

### 8. f-strings
1.  変数nameに自分の名前を代入してください。変数messageにf-stringを使用して文字列"My name is {name}."を代入し、{name}の部分を変数nameの値に置き換えて表示してください。
2.  変数ageに自分の年齢を代入してください。変数messageにf-stringを使用して文字列"I am {age} years old."を代入し、{age}の部分を変数ageの値に置き換えて表示してください。
3.  変数fruitに好きな果物の名前を代入してください。変数messageにf-stringを使用して文字列"I love {fruit}."を代入し、{fruit}の部分を変数fruitの値に置き換えて表示してください。
4.  変数priceに商品の値段を代入してください。変数messageにf-stringを使用して文字列"The price is {price} dollars."を代入し、{price}の部分を変数priceの値に置き換えて表示してください。
5.  変数quantityに商品の個数を代入してください。変数messageにf-stringを使用して文字列"You have {quantity} items."を代入し、{quantity}の部分を変数quantityの値に置き換えて表示してください。

### No.16 リスト型

1. リスト`numbers`を作成し、要素として`1, 2, 3, 4, 5`を含めてください。
2. 空のリスト`fruits`を作成してください。
3. リスト`languages`を作成し、要素として`"Python", "Java", "C++", "JavaScript", "Ruby"`を含めてください。
4. リスト`numbers`の3番目の要素を表示してください。
5. リスト`languages`の要素数を表示してください。

### No.17 リストの操作

1. リスト`numbers`に新たな要素`6`を追加してください。
2. リスト`fruits`に要素`"apple"`を追加してください。
3. リスト`numbers`から要素`4`を削除してください。
4. リスト`languages`の2番目の要素を`"Python"`から`"Go"`に変更してください。
5. リスト`fruits`の要素数を表示してください。

### No.18 リストのメソッド

1. リスト`numbers`に要素`5`が何回出現するかを表示してください。
2. リスト`fruits`の要素をアルファベット順に並べ替えてください。
3. リスト`numbers`の要素の合計値を表示してください。
4. リスト`languages`の末尾に要素`"Swift"`を追加してください。
5. リスト`fruits`から要素`"banana"`を削除してください。

### No.19 リストのコピー

1. リスト`numbers`を別のリスト`numbers_copy`にコピーしてください。
2. リスト`languages`をスライスを使って別のリスト`languages_copy`にコピーしてください。
3. リスト`fruits`を`copy()`メソッドを使って別のリスト`fruits_copy`にコピーしてください。
4. リスト`numbers`と`numbers_copy`の要素を表示して比較してください。
5. リスト`languages`と`languages_copy`の要素を表示して比較してください。

### No.20 リストの使いどころ

1. リスト`names`に友達の名前を3つ含めてください。その後、リスト`names`の要素をすべて表示してください。
2. リスト`scores`に数学のテストの得点を5つ含めてください。その後、リスト`scores`の要素の合計値を表示してください。
3. リスト`todo`に買い物リストの項目を追加してください。その後、リスト`todo`の要素数を表示してください。
4. リスト`colors`に好きな色を5つ含めてください。その後、リスト`colors`の要素をアルファベット順に並べ替えてください。

```python
colors = ["blue", "green", "red", "yellow", "orange"]
colors.sort()
print(colors)
```

5. リスト`fruits`に果物の名前を10個含めてください。その後、リスト`fruits`の中からランダムに1つの要素を選んで表示してください。

### No.21 タプル型

1. タプル`numbers`を作成し、要素として`1, 2, 3, 4, 5`を含めてください。
2. タプル`fruits`を作成し、要素として`"apple", "banana", "orange"`を含めてください。
3. タプル`coordinates`を作成し、要素として`(10, 20)`を含めてください。
4. タプル`numbers`の3番目の要素を表示してください。
5. タプル`fruits`の要素数を表示してください。

### No.22 タプルのアンパッキング

1. タプル`person`を作成し、要素として`"John", 25, "USA"`を含めてください。その後、タプルの要素を順番に取り出し、変数`name`、`age`、`country`に代入してそれぞれ表示してください。
2. タプル`point`を作成し、要素として`(3, 5)`を含めてください。その後、タプルの要素を順番に取り出し、変数`x`、`y`に代入してそれぞれ表示してください。
3. タプル`date`を作成し、要素として`(2023, 5, 15)`を含めてください。その後、タプルの要素を順番に取り出し、変数`year`、`month`、`day`に代入してそれぞれ表示してください。
4. タプル`measurement`を作成し、要素として`(5.2, 3.8, 7.1)`を含めてください。その後、タプルの要素を順番に取り出し、変数`length`、`width`、`height`に代入してそれぞれ表示してください。
5. タプル`data`を作成し、要素として`("John", 25, "USA", "john@example.com")`を含めてください。その後、タプルの要素を順番に取り出し、変数`name`、`age`、`country`、`email`に代入してそれぞれ表示してください。

### No.23 タプルの使いどころ

1. タプル`menu`を作成し、要素として`("pizza", "pasta", "salad")`を含めてください。その後、タプル`menu`の要素を順番に表示してください。
2. タプル`colors`を作成し、要素として`("red", "green", "blue")`を含めてください。その後、タプル`colors`の要素数を表示してください。
```python
colors = ("red", "green", "blue")
print(len(colors))
```

3. タプル`coordinates`を作成し、要素として`(10, 20)`を含めてください。その後、タプルの要素を順番に取り出して変数`x`と`y`に代入し、`x`と`y`の値を表示してください。
```python
coordinates = (10, 20)
x, y = coordinates
print(x)
print(y)
```

4. タプル`person1`と`person2`を作成し、要素として`(name, age, country)`の形式で値を含めてください。その後、2つのタプルの要素を比較して同じ名前かどうかを判定し、結果を表示してください。
```python
person1 = ("John", 25, "USA")
person2 = ("Jane", 30, "USA")

if person1[0] == person2[0]:
    print("Same name")
else:
    print("Different names")
```

5. タプル`dimensions`を作成し、要素として`(width, height, depth)`の形式で値を含めてください。その後、タプルの要素を順番に取り出して変数`w`、`h`、`d`に代入し、変数`w`と`h`の積を計算して表示してください。
```python
dimensions = (5, 10, 3)
w, h, d = dimensions
area = w * h
print(area)
```

## No.24 辞書型

1. 辞書`person`を作成し、キーとして`"name"`、`"age"`、`"country"`を使用し、対応する値として`"John"`、`25`、`"USA"`を含めてください。
2. 空の辞書`fruits`を作成してください。
3. 辞書`book`を作成し、キーとして`"title"`、`"author"`、`"year"`を使用し、対応する値として好きな本の情報を含めてください。
4. 辞書`person`の`"name"`キーに対応する値を表示してください。
5. 辞書`book`の要素数を表示してください。

## No.25 辞書型のメソッド

1. 辞書`person`のキーのリストを表示してください。
2. 辞書`fruits`の値のリストを表示してください。
3. 辞書`book`にキー`"genre"`と値`"Mystery"`を追加してください。
4. 辞書`person`からキー`"age"`を削除してください。
5. 辞書`fruits`のキーと値のペアを表示してください。

## No.26 辞書のコピー

1. 辞書`person`を別の辞書`person_copy`にコピーしてください。
2. 辞書`book`を`copy()`メソッドを使って別の辞書`book_copy`にコピーしてください。
3. 辞書`fruits`を`dict()`関数を使って別の辞書`fruits_copy`にコピーしてください。
4. 辞書`person`と`person_copy`の要素を表示して比較してください。
5. 辞書`book`と`book_copy`の要素を表示して比較してください。

## No.27 辞書の使いどころ

1. 辞書`student`を作成し、キーとして`"name"`、`"age"`、`"grade"`を使用し、対応する値として好きな学生の情報を含めてください。
2. 辞書`car`を作成し、キーとして`"make"`、`"model"`、`"year"`を使用し、対応する値として好きな車の情報を含めてください。
3. 辞書`product`を作成し、キーとして`"name"`、`"price"`、`"quantity"`を使用し、対応する値として商品の情報を含めてください。
4. 辞書`country`を作成し、キーとして`"name"`、`"population"`、`"capital"`を使用し、対応する値として国の情報を含めてください。
5. 辞書`person`に`"email"`キーと`"john@example.com"`の値を追加してください。

## No.28 集合型

1. 空の集合`fruits`を作成してください。
2. 集合`numbers`を作成し、要素として`1, 2, 3, 4, 5`を含めてください。
3. 集合`colors`を作成し、要素として`"red", "blue", "green", "yellow"`を含めてください。
4. 集合`fruits`に要素`"apple"`を追加してください。
5. 集合`numbers`から要素`3`を削除してください。

## No.29 集合のメソッド

1. 集合`numbers`に要素`6`を追加してください。
2. 集合`colors`に要素`"orange"`を追加してください。
3. 集合`fruits`と集合`colors`の積集合を表示してください。
4. 集合`numbers`と集合`colors`の和集合を表示してください。
5. 集合`fruits`から要素`"banana"`を削除してください。

## No.30 集合の使いどころ

1. 集合`students`を作成し、要素としてクラスの学生の名前を含めてください。
2. 集合`winners`を作成し、要素としてスポーツの優勝者の名前を含めてください。
3. 集合`prime_numbers`を作成し、要素として素数を含めてください。
4. 集合`vowels`を作成し、要素として英語の母音を含めてください。
5. 集合`fruits`と集合`vegetables`を結合して、食材の集合を作成してください。

